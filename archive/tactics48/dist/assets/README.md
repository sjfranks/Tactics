# Game assets

Place future sprites, portraits, maps, sound effects, and music in this folder. Keep filenames lowercase and use hyphens, for example `alden-idle.png`.

The prototype currently uses lightweight interface tokens so it loads quickly on mobile. Future artwork can be referenced with relative paths such as `./assets/alden-idle.png`.
