{
  "name": "tactics",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/write-background.mjs && vite",
    "build": "node scripts/write-background.mjs && vite build",
    "typecheck": "tsc -b",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.1.0"
  }
}
